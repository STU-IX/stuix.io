extends layout.pug
block head
	title STUIX - Technology United Internet Exchanges
block content
	.window
		.title-bar
			.title-bar-text Home
			.title-bar-controls
				button(aria-label='Minimize')
				button(aria-label='Maximize')
				button(aria-label='Close')
		.window-body
			img(src="https://raw.githubusercontent.com/STU-IX/branding/main/Logo/Logo.svg",width="256px")
			br
			br
			p This is a non-profit IX (Internet Exchange) 
                                     
			section.field-row(style='justify-content: flex-end')
				a(href='/join.html')
					button Join STUIX!
